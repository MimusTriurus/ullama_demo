# 2. Agent knowledge base:

Перед тем как отправить запрос игрока в LLM, мы анализируем этот запрос (что игрок хочет от NPC), извлекаем из базы знаний NPC факты игрового мира и добавляем их к запросу.

Так LLM будет отвечать, опираясь на реальное состояние игры. Осталось разобраться, что мы называем "базой знаний", потому что устроена она не совсем так, как принято в класическом RAG...

***

## Что на самом деле лежит в базе знаний

Обычно RAG работает с документами: текст режут на фрагменты, складывают в векторную базу, а на запрос достают самые похожие куски и подмешивают их в промпт. Наша база устроена иначе — она **не хранит&#x20;**&#x43D;е информацию о мире игры. В ней лежат только **намерения игрока** — то, чего он добивается от NPC своим запросом. Каждое намерение — это сигнатура вида "действие + параметры" (поле **intention**) с привязкой к своему **поставщику контекста&#x20;**(поле **action**):

```json
[
  { "intention": "SellItem | item: pistol",  "action": { "name": "SellItem",  "parameters": { "item": "pistol" } } },
  { "intention": "SellItem | item: shotgun", "action": { "name": "SellItem",  "parameters": { "item": "shotgun" } } }
]
```

Почему так, а не "сложить в базу все факты": факты меняются в рантайме (товар купили, деньги потратили, игрока ранили). Хранить их копию в базе — значит гарантированно рассинхронизировать её с игрой. Поэтому база отвечает лишь на вопрос **"чего добивается игрок"**, а сами факты подтягиваются **из игрового состояния** в момент ответа. Т.о. информация всегда актуальна.

***

## Как распознаётся намерение игрока

Один раз, при инициализации NPC, все намерения из базы прогоняются через embedding модель (поле **intention** из json выше) и преобразуются в векторный индекс. Дальше на каждый запрос игрока работает семантический поиск:

```mermaid
flowchart TD
    subgraph init["Однократно — при инициализации NPC"]
        direction LR
        SRC["intentions из базы знаний"] -- эмбеддер --> IDX[("векторный индекс")]
    end

    subgraph run["На каждый запрос игрока"]
        Q(["продай-ка дробовик"]) -- эмбеддинг фразы --> SEARCH["семантический поиск"]
        SEARCH --> HIT["ближайшее намерение:<br/>SellItem (item: shotgun)"]
        HIT --> DG["DataGetter<br/>поставщик контекста"]
        DG --> US["состояние игрока:<br/>ранен, золота 250"]
        DG --> NS["состояние NPC:<br/>дробовик: 12 шт, цена 300"]
        US --> PROMPT(["контекст — в промпт модели"])
        NS --> PROMPT
    end

    IDX -. поиск идёт по индексу .-> SEARCH

    classDef store fill:#e8f0fe,stroke:#3b6fb6,stroke-width:2px,color:#12243b
    classDef state fill:#fff3e0,stroke:#d9822b,stroke-width:2px,color:#3b2a12
    classDef edge fill:#f3f4f6,stroke:#8b8f96,stroke-width:1.5px,color:#22262b

    class SRC,IDX store
    class US,NS state
    class Q,PROMPT edge
```

> 💡 *Что такое embeddings*
>
> *Компьютер понимает не смысл слов, а числа. Embedding модель (не та, что ведёт диалог с игроком) преобразует кусочек текста в вектор так, что близкие по смыслу фразы получают близкие векторы (считается косинусное сходство, евклидово расстояние или скалярное произведение на выбор).*
>
> *Фразы "Продай дробовик" и "Дед, продай ружье" по буквам разные, а по смыслу — одно и то же, поэтому их векторы окажутся "рядом" (а вектор для запроса "Продай патроны" окажется гараздо "дальше").*

Поиск при этом **идёт по смыслу**: десяток формулировок одной просьбы ("беру помповое", "дай двустволку") ведут к одному намерению ("SellItem | item: shotgun").

***

## Откуда берётся контекст

У каждого **intention&#x20;**&#x435;сть свой **поставщик контекста (DataGetter)** с двумя каналами: **состояние NPC** (*что у торговца на складе, почём*) и **состояние игрока** (*сколько золота*). Именно они дают те самые "живые" факты — и именно здесь база "синхронизируется" с игрой.

**Откуда брать данные, решает разработчик.** Сейчас **DataGetter&#x20;**&#x43E;прашивает игровую логику напрямую (инвентарь, деньги, здоровье), но с тем же успехом мог бы "ходить" в базу данных, во внешний сервис или в другой RAG. Поменять источник == добавить новую имплементацию DataGetter. Тривиальная задача не затрагивающая ни LLM, ни остальной пайплайн.

***

## Что уходит в промпт LLM

Оба канала контекста вместе с самим запросом образуют вход модели:

```text
   request_of_user: "а дробовик есть?"
   state_of_user:   "ранен, денег 250$"
   state_of_npc:    "дробовик: 12 шт, цена 300$"
```

LLM видит только готовые строки; как разработчик их собрал, ей неважно.

***

В результате NPC отвечает с опорой на факты игрового мира. Это если и не исключает, то минимизирует вероятность галюцинаций LLM и обмана со стороны игрока. Контекст на входе - единственный источник истины для LLM.
