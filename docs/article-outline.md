# Статья, часть 2 — рабочий план

**Тема:** от «говорящей головы» (PoC из части 1) к живому и полезному NPC.
**Часть 1:** https://habr.com/ru/articles/807561/ (PoC: UE 5.3, Vosk + Mistral 7B + Piper + Oculus LipSync + MetaHuman).

## Параметры (согласовано)

- **Гвоздь двойной:** NPC, который *действует* (function calling) + NPC, который *живёт* (RAG + эмоции).
- **Формат:** мало кода, много схем. Единственный уместный листинг — формат действий / GBNF.
- **Аудитория:** смешанная (как ч.1) — бережно объясняем термины и со стороны LLM, и со стороны UE.
- **Сквозной пример:** один — NPC-торговец.

## Сквозная идея

> В части 1 персонаж научился *говорить*. В части 2 он научился **помнить, чувствовать и действовать** — перестал быть «говорящей головой» и стал участником игры.

Две несущие колонны:
- **Живой** → RAG (память фактов) + эмоции (голос / мимика).
- **Полезный** → function calling (влияет на игровой мир).

## Арка трилогии (важно держать в голове)

Финал ч.2 (раздел «грабли») готовит почву для ч.3:

> Маленькие модели ошибаются и плохо тянут длинную историю чата → в части 3 автоматически дообучаем **LoRA-адаптер под конкретного NPC**, опираясь на его описание в DataAsset.

Крючки, которые уже есть в коде и их стоит «подсветить» заранее:
- `FULlamaInferenceConfig.LoraAdapterFilePath` — слот под адаптер уже заложен.
- Data-driven описание NPC (`UNPCDataAsset`) — будущий источник обучающей выборки.

---

## Структура (актуальная, разделы = отдельные файлы в `docs/`)

Нумерация разделов = нумерация в статье. Каждый раздел — свой файл `section-0N-*.md`.

### 2. С высоты птичьего полёта — `section-02-overview.md`
- 2.1 Проблемы PoC (4 боли).
- 2.2 Наши решения: таблица «боль → решение → раздел» (= оглавление).
- 2.3 Схема полного пайплайна (STT → RAG → LLM → эмоция/действие → TTS/LipSync/MVVM).
- 2.4 Архитектура: четыре слоя (перенесено из бывшего раздела 7). Карта, детали — в разделах 3–5.
- 2.5 Масштабируемость: «сколько стоит второй NPC» + разделяемая модель в памяти.

### 3. Память NPC — `section-03-memory.md`
- Проблема «золотой рыбки» (ирония: дедушка с Альцгеймером).
- База знаний = набор триггеров; эмбеддинги (врезка для новичков) → тема запроса.
- Триггер → DataGetter → контекст (GetNpcState/GetUserState). Универсальность источника.
- Роль базы: контекст, НЕ выбор действия (пример OutOfStock/NotEnoughGoldToBuy).

### 4. Эмоции NPC — `section-04-emotions.md`
- Эмоция = реакция на игрока (через `state_of_user`).
- Структурированный ответ (emotion/answer/action).
- Эмоция → клонирование голоса + мимика + LipSync (voice cloning из сэмплов).

### 5. NPC, который действует — `section-05-function-calling.md` *(гвоздь)*
- 5.1 Проблема: из текста → машинная команда; действие только из JSON.
- 5.2 GBNF-грамматика (реальная, из `inference_cfg.json`) + генерация из DataAsset. Единственный листинг.
- 5.3 ActionHandler → MVVM (меняем данные, не пиксели).

### 6. Сквозной пример — торговец — `section-06-trader-walkthrough.md` *(кульминация)*
- Видео-центричный: минимум схем, доказательство в роликах.
- 6.1 Знакомство + скриншот DataAsset; 6.2 базовый диалог; 6.3 разные исходы (OutOfStock/NotEnoughGold); 6.4 вопрос не по адресу (DoNothing); 6.5 попытка обмана (модель не проведёшь); 6.6 итог.

### 7. Грабли + мост в часть 3 — `section-07-pitfalls-and-part3.md`
- 7.1 Главная боль: без LoRA точность низкая (грамматика чинит форму, не смысл).
- 7.2 Ответ: автоматизированный MLOps-пайплайн (датасет из DataAsset → LoRA → оценка → версионирование) + эмбеддинги. Тизер части 3.
- Итог части 2.

> Вступление/рекап (ч.1 → ч.2, «говорящая голова → живой участник») — во введении перед разделом 2; отдельного файла пока нет.

---

## Открытые вопросы / TODO для наполнения
- [ ] Подобрать/записать гифку с торговцем для вступления.
- [ ] Уточнить фактуру раздела 7.1 (конкретные примеры ошибок модели; цифры «до/после» LoRA из `3_evaluation.py`).
- [ ] Доработать схемы: пайплайн (2.3), четыре слоя (2.4), масштабирование (2.5), RAG-поток, эмоция→голос→лицо.
- [ ] Записать видео для раздела 6 (базовый диалог, разные исходы, вопрос не по адресу, попытка обмана) + скриншот DataAsset.
- [x] Формат листинга GBNF в разделе 5.2 — вставлена реальная грамматика из `inference_cfg.json`.
- [x] Архитектуру (бывший раздел 7) свернули в 2.4–2.5; отдельного файла больше нет.
- [ ] Факт-чек: мимика по эмоции (раздел 4), `state_of_npc` в промпте (раздел 6), реальные цены/реплики торговца.
- [x] STT: реализован на Qwen-STT (в ч.1 был Vosk). Самого STT-модуля в коде нет — описываем как внешний компонент.
